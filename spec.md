# 메일 타래 보기 + 답장 초안 화면 기획서

> 연습용 화면입니다. 실제 회사 자료는 쓰지 않고, `02_instructors.csv`에 있는 가짜 예시 자료만 씁니다.
> 화면은 한 장, 버튼은 하나, 결과는 한 종류입니다. 저장 기능과 로그인·회원 구분은 넣지 않습니다.

## 목표

교육과정 담당자가 **강사님 한 분과 지금까지 무슨 이야기를 주고받았는지 찾는 시간**과
**답장을 처음부터 쓰는 시간**을 줄입니다.

지금은 강사님 이름으로 메일함을 검색하고, 지난 메일을 위아래로 훑어 어디까지 이야기됐는지 확인한 뒤,
빈 화면에 답장을 새로 쓰는 순서입니다. 한 분당 몇 분씩 걸리고, 12분이면 그만큼 반복됩니다.

이 화면에서는 강사님을 고르는 순간 지난 메일이 말풍선으로 한 번에 보이고,
버튼 하나로 그 맥락에 맞는 답장 초안이 나옵니다. 담당자는 초안을 읽고 고쳐 쓰기만 하면 됩니다.

## 입력

| 입력 | 형태 | 어디서 오나 |
|---|---|---|
| 강사 목록 | `02_instructors.csv` (성함 · 메일 · 담당모듈 · 일정 · 장소 · 시수 · 시간당강사료 · 상태) | 이미 있는 예시 파일 |
| 메일 타래 예시 | 강사 12분 × 3~4통, 모두 45통의 가짜 메일 (보낸사람 / 날짜 / 제목 / 본문 2~3줄) | `index.html` 안의 `THREADS` 자료 |
| 담당자가 고른 강사 한 명 | 왼쪽 목록에서 카드 클릭 | 화면에서 |
| 버튼 누름 | `답장 초안 만들기` 버튼 클릭 | 화면에서 |

담당자가 직접 타이핑해서 넣는 값은 없습니다. 고르고 누르는 것이 전부입니다.

## 단계

1. **화면을 연다** — `index.html`을 브라우저로 엽니다. 왼쪽에 강사 12분의 카드 목록이 뜨고, 오른쪽은 "강사님을 골라 주세요"라는 안내만 있는 빈 자리입니다.
2. **강사님을 고른다** — 카드를 클릭하면 그 강사님의 메일 타래를 예시 자료에서 찾아옵니다.
3. **말풍선으로 보여준다** — 찾아온 메일을 날짜 순서대로 오른쪽에 그립니다. 담당자가 보낸 메일은 오른쪽 정렬, 강사님이 보낸 메일은 왼쪽 정렬입니다. 각 말풍선 위에 보낸사람과 날짜를 작게 답니다.
4. **버튼을 누른다** — 말풍선 아래 `답장 초안 만들기` 버튼 하나가 있습니다. 화면 안의 버튼은 이것뿐입니다.
5. **초안을 만든다** — 마지막 메일이 누구에게서 왔는지와 그 강사님의 **상태** 값을 보고, 미리 정해 둔 문장 틀 중 하나를 골라 이름·모듈·일정·장소를 끼워 넣습니다.
   - 강의안 회신 대기 → 강의안 마감 리마인드
   - 섭외 회신 대기 → 섭외 재확인
   - 일정 조율 중 → 대체 일정 제안
   - 확정 → 확정 안내 및 준비물 안내
   - 종료 → 마무리 인사
6. **초안을 보여준다** — 말풍선 줄 맨 아래에 "초안" 표시가 붙은 회색 말풍선으로 붙입니다. 버튼을 다시 누르면 그 초안 하나만 새로 바뀝니다. 초안이 쌓이지는 않습니다.
7. **끝난다** — 담당자가 초안을 읽고 필요한 부분을 복사해 갑니다. 화면은 아무것도 저장하지 않고, 다른 강사님을 고르면 처음 상태로 돌아갑니다.

## 출력

결과는 **답장 초안 말풍선 한 종류**뿐입니다. 파일도, 다운로드도, 메일 발송도 없습니다.

### 화면에 무엇이 몇 개 보이나

강사님을 고른 뒤, 화면 전체에 보이는 덩어리는 **5가지**입니다.

| # | 덩어리 | 개수 | 비고 |
|---|---|---|---|
| 1 | 왼쪽 강사 카드 | **12개** (고정) | `02_instructors.csv` 12행과 1:1. 고른 카드 1개만 테두리가 진해집니다 |
| 2 | 오른쪽 위 강사 요약 | **1개 / 2줄** | 1줄 = 성함 · 담당모듈, 2줄 = 일정 · 장소 · 상태 |
| 3 | 지난 메일 말풍선 | **3~4개** (강사님마다 다름) | 예시 자료에 들어 있는 통수 그대로. 12분 합계 45통 |
| 4 | `답장 초안 만들기` 버튼 | **1개** | 화면 안의 버튼은 이것뿐 |
| 5 | 답장 초안 말풍선 | **1개** | 버튼을 다시 눌러도 이 1개가 새 내용으로 바뀔 뿐, 늘지 않습니다 |

강사님을 고르기 전에는 1번 12개 + 안내 문구 1줄만 보이고, 2·3·4·5번은 화면에 없습니다.

### 표는 몇 개 열인가

**화면에 표는 없습니다.** 왼쪽은 카드 목록, 오른쪽은 말풍선이라 행·열로 된 표를 쓰지 않습니다.
다만 카드와 말풍선에 들어가는 항목은 아래로 고정합니다.

- **강사 카드 (12개 각각) — 4개 항목**: `성함` · `담당모듈` · `일정` · `상태`
  (`02_instructors.csv`의 8개 열 중 4개만 씁니다. `메일`은 초안에서만, `장소`는 요약줄에서만, `시수`·`시간당강사료`는 화면에 쓰지 않습니다.)
- **지난 메일 말풍선 (1개 각각) — 4개 항목**: `보낸사람` · `날짜` · `제목` · `본문`
  (보낸사람이 담당자면 오른쪽 정렬, 강사님이면 왼쪽 정렬)

### 글이라면 몇 줄인가

답장 초안 말풍선은 **머리말 2줄 + 본문 6줄 = 정확히 8줄**입니다. 길이는 강사님이 누구든 항상 같습니다.

| 줄 | 무엇 | 예시 |
|---|---|---|
| 1 | 받는 사람 | `받는 사람: 최유라 (yura.choi@example.com)` |
| 2 | 제목 | `제목: [데이터 실무 과정] M4 강의안 관련 안내` |
| 3 | 인사 | `안녕하세요, 최유라 강사님. 교육운영 담당자입니다.` |
| 4 | 배경 (모듈·일정·장소) | `9월 5일 (토) 마포 교육장에서 진행하는 M4 모듈 관련하여 안내드립니다.` |
| 5 | 용건 (상태별 문장 5종 중 1줄) | `보내주시기로 한 강의안을 아직 받지 못하여 다시 안내드립니다.` |
| 6 | 요청 | `8월 29일 (금)까지 회신 주시면 감사하겠습니다.` |
| 7 | 여지 | `일정이나 내용 조정이 필요하시면 편히 말씀해 주세요.` |
| 8 | 맺음 | `감사합니다.` |

- 3·4·7·8줄은 이름·모듈·일정·장소만 바뀌고 문장 틀은 늘 같습니다.
- 5줄만 상태에 따라 5종 중 하나가 골라집니다 (대기 2종 · 조율 · 확정 · 종료).
- 한 줄은 화면에서 최대 2행까지만 접히도록 하고, 8줄을 넘기지 않습니다.

### 화면 모양

```
┌───────────────┬──────────────────────────────────────────────────┐
│ 강사 목록 12개 │ ② 최유라 · M4 기획자를 위한 금융데이터 활용 실무  │
│               │    9월 5일 (토) · 마포 교육장 · 강의안 회신 대기  │
│ ▸ 최유라 ●    │ ───────────────────────────────────────────────  │
│   김도현      │ ③ 말풍선 2~4개              ┌───────────┐ 담당자 │
│   박서연      │                             │ 확정 안내… │ 8/21  │
│   이준호      │                             └───────────┘        │
│   정민아      │    강사님 8/22                                    │
│   한지훈 ●    │    ┌───────────────┐                             │
│   오세영      │    │ 네, 확인했습니다 │                             │
│   배수민      │    └───────────────┘                             │
│   문태경      │                                                  │
│   류해린      │ ④        [ 답장 초안 만들기 ]  ← 버튼 1개         │
│   신도윤      │ ⑤ ┌──────────────────────────────────┐ 초안      │
│   장예은      │   │ 1 받는 사람: 최유라 (yura.choi@…) │           │
│  (12명 고정)  │   │ 2 제목: [데이터 실무 과정] M4 …    │           │
│               │   │ 3 안녕하세요, 최유라 강사님…      │           │
│               │   │ 4 9월 5일 (토) 마포 교육장에서…   │           │
│               │   │ 5 강의안을 아직 받지 못하여…      │           │
│               │   │ 6 8월 29일 (금)까지 회신 주시면…  │           │
│               │   │ 7 조정이 필요하시면 편히…         │           │
│               │   │ 8 감사합니다.                     │           │
│               │   └──────────────────────────────────┘ 항상 8줄  │
└───────────────┴──────────────────────────────────────────────────┘
```

- 색은 지금 `index.html`에서 쓰는 상태 색(주황 = 대기, 파랑 = 조율, 초록 = 확정, 회색 = 종료)을 그대로 씁니다.
- 화면 폭이 좁으면 왼쪽 목록이 위로 올라가고 말풍선이 아래에 놓입니다.
- 강사님을 고르기 전에는 오른쪽에 안내 문구 한 줄만 있고, 버튼은 보이지 않습니다.
